# Disabled: the app is deployed as a single ASP.NET Core host (API + SPA from wwwroot).
# Use .github/workflows/main_mission13.yml instead. Re-enable only if you split the frontend to Azure Static Web Apps again.

name: Azure Static Web Apps CI/CD (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Use main_mission13.yml for App Service deployment."
